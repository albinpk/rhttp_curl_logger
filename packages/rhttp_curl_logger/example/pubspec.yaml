name: example
description: "A new Flutter project."
publish_to: "none"
version: 0.2.0

resolution: workspace

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  rhttp: ^0.16.0
  rhttp_curl_logger: ^0.2.1+2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
